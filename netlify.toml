# Netlify config: app lives in lims-health-ai-web
[build]
  base = "lims-health-ai-web"
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
